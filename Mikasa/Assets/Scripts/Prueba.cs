using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Prueba : MonoBehaviour
{
    public Prueba1 obj;
    
   
 

    // Start is called before the first frame update
    void Start()
    {
        obj = new Prueba1();
        //obj._nombre = "Santiago";
        //obj._apellido = "Henao";

        //obj.Saludo(obj._nombre,obj._apellido);
        //obj.Saludo(obj._nombre);


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
